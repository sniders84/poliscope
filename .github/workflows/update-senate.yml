name: Senate Workflow

on:
  schedule:
    - cron: '0 6 * * *'   # runs daily at 6 AM UTC
  workflow_dispatch:

jobs:
  senate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Bootstrap senators
        run: node scripts/bootstrap-senators.js

      - name: Scrape legislation (119th Congress sponsored/cosponsored)
        run: node scripts/legislation-scraper.js

      # ⚠️ Disabled until you have these scripts implemented
      # - name: Scrape committees
      #   run: node scripts/committee-senators-scraper.js

      # - name: Scrape votes
      #   run: node scripts/votes-senators-scraper.js

      - name: Merge senator data
        run: node scripts/merge-senators.js

      # ⚠️ Disabled until senators-score.js exists
      # - name: Score senators
      #   run: node scripts/senators-score.js
